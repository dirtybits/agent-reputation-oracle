# USDC regression test skill

This skill exists only to verify that repo-backed USDC listings still render as USDC in production.

## Usage

- Confirm the marketplace card shows a USDC price.
- Confirm the skill detail page shows USDC as the primary price.
- Confirm the recent activity feed records the listing and purchase in USDC.