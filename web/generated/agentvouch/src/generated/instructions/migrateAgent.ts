/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAgentProfilePda } from "../pdas";
import { AGENTVOUCH_PROGRAM_ADDRESS } from "../programs";

export const MIGRATE_AGENT_DISCRIMINATOR = new Uint8Array([
  102, 150, 249, 223, 92, 169, 131, 39,
]);

export function getMigrateAgentDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    MIGRATE_AGENT_DISCRIMINATOR,
  );
}

export type MigrateAgentInstruction<
  TProgram extends string = typeof AGENTVOUCH_PROGRAM_ADDRESS,
  TAccountAgentProfile extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAgentProfile extends string
        ? WritableAccount<TAccountAgentProfile>
        : TAccountAgentProfile,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type MigrateAgentInstructionData = {
  discriminator: ReadonlyUint8Array;
  metadataUri: string;
};

export type MigrateAgentInstructionDataArgs = { metadataUri: string };

export function getMigrateAgentInstructionDataEncoder(): Encoder<MigrateAgentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["metadataUri", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
    ]),
    (value) => ({ ...value, discriminator: MIGRATE_AGENT_DISCRIMINATOR }),
  );
}

export function getMigrateAgentInstructionDataDecoder(): Decoder<MigrateAgentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["metadataUri", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
  ]);
}

export function getMigrateAgentInstructionDataCodec(): Codec<
  MigrateAgentInstructionDataArgs,
  MigrateAgentInstructionData
> {
  return combineCodec(
    getMigrateAgentInstructionDataEncoder(),
    getMigrateAgentInstructionDataDecoder(),
  );
}

export type MigrateAgentAsyncInput<
  TAccountAgentProfile extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * because the stored bump may be at a stale offset after a struct layout change.
   * Seeds and owner are validated; we rewrite the account data manually.
   */
  agentProfile?: Address<TAccountAgentProfile>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  metadataUri: MigrateAgentInstructionDataArgs["metadataUri"];
};

export async function getMigrateAgentInstructionAsync<
  TAccountAgentProfile extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENTVOUCH_PROGRAM_ADDRESS,
>(
  input: MigrateAgentAsyncInput<
    TAccountAgentProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  MigrateAgentInstruction<
    TProgramAddress,
    TAccountAgentProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENTVOUCH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    agentProfile: { value: input.agentProfile ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.agentProfile.value) {
    accounts.agentProfile.value = await findAgentProfilePda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("agentProfile", accounts.agentProfile),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getMigrateAgentInstructionDataEncoder().encode(
      args as MigrateAgentInstructionDataArgs,
    ),
    programAddress,
  } as MigrateAgentInstruction<
    TProgramAddress,
    TAccountAgentProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type MigrateAgentInput<
  TAccountAgentProfile extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /**
   * because the stored bump may be at a stale offset after a struct layout change.
   * Seeds and owner are validated; we rewrite the account data manually.
   */
  agentProfile: Address<TAccountAgentProfile>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
  metadataUri: MigrateAgentInstructionDataArgs["metadataUri"];
};

export function getMigrateAgentInstruction<
  TAccountAgentProfile extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof AGENTVOUCH_PROGRAM_ADDRESS,
>(
  input: MigrateAgentInput<
    TAccountAgentProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): MigrateAgentInstruction<
  TProgramAddress,
  TAccountAgentProfile,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? AGENTVOUCH_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    agentProfile: { value: input.agentProfile ?? null, isWritable: true },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("agentProfile", accounts.agentProfile),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getMigrateAgentInstructionDataEncoder().encode(
      args as MigrateAgentInstructionDataArgs,
    ),
    programAddress,
  } as MigrateAgentInstruction<
    TProgramAddress,
    TAccountAgentProfile,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedMigrateAgentInstruction<
  TProgram extends string = typeof AGENTVOUCH_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /**
     * because the stored bump may be at a stale offset after a struct layout change.
     * Seeds and owner are validated; we rewrite the account data manually.
     */
    agentProfile: TAccountMetas[0];
    authority: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: MigrateAgentInstructionData;
};

export function parseMigrateAgentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedMigrateAgentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      agentProfile: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getMigrateAgentInstructionDataDecoder().decode(instruction.data),
  };
}
