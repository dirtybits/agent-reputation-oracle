/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findConfigPda } from "../pdas";
import { REPUTATION_ORACLE_PROGRAM_ADDRESS } from "../programs";

export const ADMIN_MIGRATE_AGENT_DISCRIMINATOR = new Uint8Array([
  203, 79, 102, 176, 194, 111, 179, 208,
]);

export function getAdminMigrateAgentDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    ADMIN_MIGRATE_AGENT_DISCRIMINATOR,
  );
}

export type AdminMigrateAgentInstruction<
  TProgram extends string = typeof REPUTATION_ORACLE_PROGRAM_ADDRESS,
  TAccountAgentProfile extends string | AccountMeta<string> = string,
  TAccountConfig extends string | AccountMeta<string> = string,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAgentProfile extends string
        ? WritableAccount<TAccountAgentProfile>
        : TAccountAgentProfile,
      TAccountConfig extends string
        ? ReadonlyAccount<TAccountConfig>
        : TAccountConfig,
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type AdminMigrateAgentInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type AdminMigrateAgentInstructionDataArgs = {};

export function getAdminMigrateAgentInstructionDataEncoder(): FixedSizeEncoder<AdminMigrateAgentInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: ADMIN_MIGRATE_AGENT_DISCRIMINATOR }),
  );
}

export function getAdminMigrateAgentInstructionDataDecoder(): FixedSizeDecoder<AdminMigrateAgentInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getAdminMigrateAgentInstructionDataCodec(): FixedSizeCodec<
  AdminMigrateAgentInstructionDataArgs,
  AdminMigrateAgentInstructionData
> {
  return combineCodec(
    getAdminMigrateAgentInstructionDataEncoder(),
    getAdminMigrateAgentInstructionDataDecoder(),
  );
}

export type AdminMigrateAgentAsyncInput<
  TAccountAgentProfile extends string = string,
  TAccountConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** validate its owner/discriminator/PDA manually before rewriting it. */
  agentProfile: Address<TAccountAgentProfile>;
  config?: Address<TAccountConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getAdminMigrateAgentInstructionAsync<
  TAccountAgentProfile extends string,
  TAccountConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof REPUTATION_ORACLE_PROGRAM_ADDRESS,
>(
  input: AdminMigrateAgentAsyncInput<
    TAccountAgentProfile,
    TAccountConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  AdminMigrateAgentInstruction<
    TProgramAddress,
    TAccountAgentProfile,
    TAccountConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? REPUTATION_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    agentProfile: { value: input.agentProfile ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.config.value) {
    accounts.config.value = await findConfigPda();
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("agentProfile", accounts.agentProfile),
      getAccountMeta("config", accounts.config),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAdminMigrateAgentInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminMigrateAgentInstruction<
    TProgramAddress,
    TAccountAgentProfile,
    TAccountConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type AdminMigrateAgentInput<
  TAccountAgentProfile extends string = string,
  TAccountConfig extends string = string,
  TAccountAuthority extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** validate its owner/discriminator/PDA manually before rewriting it. */
  agentProfile: Address<TAccountAgentProfile>;
  config: Address<TAccountConfig>;
  authority: TransactionSigner<TAccountAuthority>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getAdminMigrateAgentInstruction<
  TAccountAgentProfile extends string,
  TAccountConfig extends string,
  TAccountAuthority extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof REPUTATION_ORACLE_PROGRAM_ADDRESS,
>(
  input: AdminMigrateAgentInput<
    TAccountAgentProfile,
    TAccountConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): AdminMigrateAgentInstruction<
  TProgramAddress,
  TAccountAgentProfile,
  TAccountConfig,
  TAccountAuthority,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? REPUTATION_ORACLE_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    agentProfile: { value: input.agentProfile ?? null, isWritable: true },
    config: { value: input.config ?? null, isWritable: false },
    authority: { value: input.authority ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("agentProfile", accounts.agentProfile),
      getAccountMeta("config", accounts.config),
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getAdminMigrateAgentInstructionDataEncoder().encode({}),
    programAddress,
  } as AdminMigrateAgentInstruction<
    TProgramAddress,
    TAccountAgentProfile,
    TAccountConfig,
    TAccountAuthority,
    TAccountSystemProgram
  >);
}

export type ParsedAdminMigrateAgentInstruction<
  TProgram extends string = typeof REPUTATION_ORACLE_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** validate its owner/discriminator/PDA manually before rewriting it. */
    agentProfile: TAccountMetas[0];
    config: TAccountMetas[1];
    authority: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: AdminMigrateAgentInstructionData;
};

export function parseAdminMigrateAgentInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedAdminMigrateAgentInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      agentProfile: getNextAccount(),
      config: getNextAccount(),
      authority: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getAdminMigrateAgentInstructionDataDecoder().decode(instruction.data),
  };
}
